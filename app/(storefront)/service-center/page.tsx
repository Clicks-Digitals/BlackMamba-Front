import { ServiceCenterFeature } from "@/features/service-center";

export const metadata = {
  title: "Service Center | Black Mamba",
  description: "Contact our electronics service center for support, repairs, and inquiries.",
};

export default function ServiceCenterPage() {
  return <ServiceCenterFeature />;
}
