# Mawsouq — Rebrand Implementation Tasks

> خطة تنفيذ الهوية الجديدة (Mawsouq) على الستورفرونت.
> كل تاسك جنبه `- [ ]` — علّم عليه لما يخلص.

**الهدف:** استبدال الهوية الحالية (JoSouk — تركواز `#2E4D52` + أصفر + خطوط Satoshi/Cairo/Bebas) بهوية Mawsouq الجديدة (أزرق `#0C2DB2` + Chillax + Alexandria).

**المرجع:** الـ design system المرفق من العميل + `public/logo-dark.png` / `public/logo-white.png`.

**نطاق العمل:** `josouk-frontend` أساسًا. البانل (`josouk-panel-v2`) في القسم الأخير — منفصل وأولوية أقل.

---

## 📊 الحالة — آخر تحديث ١٩ أغسطس ٢٠٢٦

| المرحلة | الحالة |
|---|---|
| ١ — الأساس (توكنات) | 🟡 الخطوط والألوان والـ radius خلصوا · **الحركة لسه** |
| ٢ — اللوجو والأصول | ✅ **خلصت** |
| ٣ — تنظيف الألوان اليدوية | ✅ **خلصت** (~٩٥٠ تكرار في ٩٦ ملف) |
| ٤ — الخطوط في الكومبوننتس | 🟡 التبديل خلص (٤٧ ملف) · **المراجعة البصرية و sentence case لسه** |
| ٥ — الكومبوننتس | ⬜ لسه |
| ٦ — RTL | ⬜ لسه |
| ٧ — الحركة | ⬜ لسه |
| ٨ — السوشيال | ⬜ لسه (شغل تصميم) |
| ٩ — المراجعة النهائية | ⬜ لسه |
| ١٠ — البانل | ⬜ لسه |

**الخلاصة:** الهوية البصرية الأساسية (خط + لوجو + ألوان + اسم) خلصت والموقع شغال عليها. اللي فاضل هو الشكل النهائي: الزوايا، الحركة، ومراجعة الكومبوننتس.

**مفيش بند موقف شغل دلوقتي.** اللي فاضل شغل تنفيذي عادي + قرار Black Mamba.

---

## ⛔ محتاج رد من العميل قبل ما نبدأ

دي حاجات مش موجودة في الصور المرسلة، والشغل هيتوقف عندها أو هيتعمل مرتين:

- [x] ~~**ملفات اللوجو بصيغة نظيفة**~~ — ✅ اتسلمت PNG شفاف 1600×542 (`logo-dark.png` أزرق `#0C2DB2` + `logo-white.png` أبيض). اتأكدت إن الشفافية حقيقية والأزرق مطابق للـ design system.
- [x] ~~**مقاسات الـ corner radius**~~ — ✅ اتحلت باجتهاد (10px)، تتعدل في سطر واحد لو المدير بعت مقاس مختلف. الأصلي: — القسم ده كان مقصوص في الصور. التصميم واضح إن فيه زوايا مدورة (أزرار ~8px، pills دائرية كاملة، كروت ~12px) بس المقاسات الرسمية مش معانا. **مهم جدًا** لأن المشروع حاليًا `--radius: 0` (زوايا حادة تمامًا).
- [ ] **الـ spacing scale** — برضه مقصوص في الصور.
- [x] ~~**ترخيص خط Chillax**~~ — ✅ **ITF Free Font License**: استخدام تجاري مجاني + استضافة ذاتية مسموحة صراحة ("permitted and recommended"). ⚠️ **ممنوع** الـ subsetting أو تحويل الصيغة — الملف يتسيرف زي ما هو.
- [ ] **هوية Black Mamba / PC Builder** — القسم ده حاليًا sub-brand ليه خطوطه الخاصة (Beckman + Bebas Neue) وهويته المستقلة. هل يتحوّل هو كمان للهوية الجديدة ولا يفضل زي ما هو؟ لو اتحوّل، ده يضيف ~15 ملف للشغل.
- [x] ~~**تأكيد تغيير اسم البراند**~~ — ✅ اتأكد واتنفذ: `JoSouk` → `Mawsouq` في كل الـ metadata والـ alt texts، و`Mawsouk` (بالـ K) → `Mawsouq` في نصوص الفوتر.

### ⚠️ قرارات اتاخدت أثناء التنفيذ — محتاجة تأكيد المدير

**١. الهيدر والفوتر على Ink `#0B0B0D` مش أزرق.**
الأزرق على شريط بعرض الشاشة كان هيتخطى حصة الـ ١٠–١٥٪ بكتير، والـ design system بيقول حرفيًا *"if a design feels very blue, it is wrong"*. الـ Ink واحد من النيوترالز الخمسة، فده مطابق.
**بديل:** هيدر أبيض — يتطلب تعديل ٣٨ حالة `text-white` جوه كومبوننتس الهيدر.

**٢. الشريط الأصفر بقى blue tint `#E8EBFB` بنص أزرق.** الأصفر مالوش أي مقابل في الباليتة الجديدة.

**٣. الأشرطة بعرض الشاشة في الهوم بقت تينت/أبيض مش أزرق.** نفس سبب البند الأول.

**٤. البانلات الكبيرة (صفحة 404، بانل اللوجين ٤٢٪) بقت Ink مش أزرق.** نفس السبب. كروت الـ promo الأصغر فضلت زرقا كلمسة براند.

**٥. العربي فضل على Cairo ومتحولش لـ Alexandria** (بقرار من العميل، 19 أغسطس 2026).
السبب: **Chillax مفيهوش ولا حرف عربي** — اتأكدت من الملف نفسه (٣٦٣ حرف، صفر في نطاقات 0600–06FF و FB50–FEFF وكل الـ presentation forms). فالخط اللاتيني اتبدل والعربي اتساب زي ما هو مؤقتًا.
Alexandria متاح على Google Fonts بدعم عربي كامل بأوزان 400/500/600 لما يتقرر تركيبه.

> **ملاحظة:** المدير قال "ill send the exact color codes" — بس أكواد الألوان **موجودة بالفعل** بالكامل في صور الـ design system وهي اللي اتعمل عليها الخطة دي. لو بعت أكواد مختلفة، التاسك الأول بس هو اللي هيتعدل.

---

## القيم المرجعية (مستخرجة من الـ design system)

### الألوان

| التوكن | الكود | الاستخدام |
|---|---|---|
| White — surface | `#FFFFFF` | الأسطح والكروت |
| Background neutral | `#F5F6FA` | خلفية الصفحة |
| Border neutral | `#E4E7F0` | كل الحدود |
| Secondary text | `#5A5F73` | نص ثانوي |
| Ink — text | `#0B0B0D` | النص الأساسي |
| **Mawsouq Blue** | `#0C2DB2` | أزرار، لينكات، active states |
| Blue tint | `#E8EBFB` | hover / selected |
| Blue hover | `#0A2593` | hover على الأزرق |
| Blue pressed | `#081F7C` | ضغط |
| Deal accent | `#FF7A1A` | **الخصومات وأسعار التخفيض والعدادات فقط** |
| Success | `#1E9E5A` | |
| Error | `#D93025` | |
| Warning | `#B7791F` | |

**قاعدة النِسب (مُلزِمة):** ~80% نيوترال · 10–15% أزرق · 5% accent.
> "If a design feels 'very blue', it is wrong."

### الخطوط

- **Chillax** — الخط اللاتيني **الوحيد**. Semibold 600 لـ H1–H2 والأسعار · Medium 500 لـ H3 والأزرار والليبلز · Regular 400 للنص.
- **Alexandria** — كل النص العربي، بحجم أكبر **٥–١٠٪** من مقابله اللاتيني. عربية فصحى رسمية فقط · ممنوع مَيَلان (italic) مزيّف أو letterspacing أو تمديد.

| المستوى | المقاس | الوزن |
|---|---|---|
| Hero | 48–64 | Semibold |
| Heading 1 | 36 | Semibold |
| Heading 2 | 28 | Semibold |
| Heading 3 | 20 | Medium |
| Body | 16 / 1.55 | Regular |
| Small label | 13 | Medium |

- **sentence case** في كل حتة (ممنوع ALL CAPS).
- **الأسعار:** أرقام إنجليزية (Western digits) في اللغتين · سعر الكارت 18 Semibold · السعر القديم مشطوب · سعر التخفيض ممكن ياخد الـ deal accent.

### الحركة

- `cubic-bezier(.4, 0, .2, 1)` · مدة **120–200ms** · fades فقط.
- Hover/press = **تغميق** اللون. ممنوع bounces أو shrink transforms.
- العناصر النيوترال: hover → blue tint.

### الـ Sweep (الموتيف الوحيد)

خط سفلي سميك مدوّر بلون Mawsouq Blue — بيحاكي ذيل الـ **ق** في اللوجو.
يتحط تحت عناوين الأقسام أو الأرقام المهمة أو كفاصل. **يُستخدم بحذر شديد** — ده الموتيف الزخرفي **الوحيد** للبراند.

---

## المرحلة ١ — الأساس (Design Tokens)

> كل اللي بعد كده معتمد على المرحلة دي. متبدأش أي حاجة تانية قبل ما تخلص.

- [x] ~~**١.١ — تحميل وتركيب خط Chillax**~~ ✅
  - اتركب `Chillax-Variable.woff2` (محور الوزن 200–700، ٥٥KB) في `public/fonts/` — ملف واحد بيغطي 400/500/600 وأصغر من الـ ٣ ملفات الستاتيك (٦٧KB).
  - `@font-face` اتضاف في `app/globals.css`، ونسخة الترخيص في `public/fonts/Chillax-LICENSE.txt`.
  - ✅ اتشال `@import url("https://api.fontshare.com/...")` بتاع Satoshi — كان طلب خارجي render-blocking.
  - **متأكد:** الخط بيتسيرف 200 بـ 55,640 bytes ونوع `font/woff2`.

- [ ] **١.٢ — تركيب خط Alexandria** — ⏸️ مؤجل بقرار العميل (شوف قسم القرارات فوق). العربي فاضل على Cairo.
  - Alexandria متاح على Google Fonts → استخدم `next/font/google` في `app/layout.tsx` بنفس نمط `Cairo` الحالي.
  - `subsets: ["arabic", "latin"]`, `variable: "--font-alexandria-src"`.

- [x] ~~**١.٣ — استبدال متغيرات الخطوط في `app/globals.css`**~~ ✅ (الجزء اللاتيني)
  - `--font-sans` و `--font-heading` بقوا Chillax، و`--font-satoshi` اتشال، و`--font-chillax` اتضاف.
  - قاعدة `html[lang="ar"]` اتسابت على Cairo عن قصد.
  - `--font-bebas` و `--font-beckman` اتسابوا لحد ما يتقرر مصير Black Mamba.
  - **متأكد:** الـ CSS المبني بيقول `--font-sans: "Chillax"` وصفر أثر لـ Satoshi.

- [x] ~~**١.٤ — استبدال متغيرات الألوان في `:root`**~~ ✅
  - كل توكنات shadcn اتحدثت: `--primary` `#0C2DB2` · `--background` `#F5F6FA` · `--foreground` `#0B0B0D` · `--border`/`--input` `#E4E7F0` · `--secondary`/`--accent` `#E8EBFB` · `--destructive` `#D93025` · `--muted-foreground` `#5A5F73` · `--card`/`--popover` `#FFFFFF` · `--ring` `#0C2DB2`.
  - توكنات براند جديدة: `--surface` `--ink` `--secondary-text` `--blue-tint` `--blue-hover` `--blue-pressed` `--deal` `--success` `--warning`.
  - كلهم مسجلين في `@theme inline` → Tailwind بيولّد `bg-deal` و `text-ink` وهكذا.
  - الـ charts اتربطت بالباليتة بدل ألوان oklch البرتقالية القديمة.

- [x] ~~**١.٥ — تنظيف توكنات الهوية القديمة**~~ ✅
  - `--brand-one/two/three/four` اتشالوا خالص.
  - `--store-nav-bg` و `--store-footer-bg` → **Ink `#0B0B0D`** (مش أزرق — شوف القرار تحت).
  - `--store-subnav-bg` → **blue tint `#E8EBFB`** بنص أزرق (الشريط الأصفر).
  - السكرول بار اتحدث (`#c4d3d5` → `--border`، `#2e4d52` → `--primary`).

- [x] ~~**١.٦ — ضبط الـ corner radius**~~ ✅ `--radius: 10px`
  - المقاس الرسمي مجاش من العميل، فاستُخرج من القيم المكتوبة يدوي في الكومبوننتس (متجمعة حوالين 8/10/12px). الـ 10 بيخلي كل درجة رقم صحيح: sm 6 · md 8 · lg 10 · xl 14 · 2xl 18.
  - أثّر على ~٢٥٥ عنصر كانوا حادين تمامًا.

- [ ] **١.٧ — توكنات الحركة**
  - ضيف `--ease-brand: cubic-bezier(.4, 0, .2, 1)` و `--duration-fast: 120ms` / `--duration-slow: 200ms`.
  - راجع أنيميشنز `globals.css` الموجودة: `reveal-up` مدتها `1s` بـ `cubic-bezier(0.2, 0.8, 0.2, 1)` و `hero-ken-burns` مدتها `15s` — الاتنين مخالفين لقاعدة الـ 120–200ms. محتاج قرار: يتعدلوا ولا يتعتبروا استثناء للهيرو؟

---

## المرحلة ٢ — اللوجو والأصول

- [x] ~~**٢.١ — استبدال `public/logo.svg`**~~ ✅ اللوجو القديم (JoSouk بالتركوازي `#2E4D52`) اتمسح خالص.

- [x] ~~**٢.٢ — تجهيز نسخ اللوجو**~~ ✅
  - `logo-dark.png` (أزرق) و `logo-white.png` (أبيض) — من العميل، شفافين.
  - `logo-mark.png` و `logo-mark-white.png` — الـ **ق** مربعة ٥١٢×٥١٢، اتقصّت من اللوجو الكامل.

- [x] ~~**٢.٣ — تحديث `app/favicon.ico`**~~ ✅ بالـ mark، بـ ٦ مقاسات (16 → 256).

- [x] ~~**٢.٤ — تحديث استخدامات اللوجو في الكود**~~ ✅
  - الاستخدامات الفعلية طلعت **٥** مش ٨ (الباقي كان مطابقات كاذبة لكلمة `logout`):
  - `components/layout/header/header.tsx` — نص "MAW·SOUK" الأصفر → `logo-white.png`
  - `components/layout/footer/footer.tsx` — نفس النص → `logo-white.png`
  - `app/(auth)/layout.tsx` ×2 → `logo-white.png` (البانل الغامق) و `logo-dark.png` (الموبايل)
  - `app/not-found.tsx` → `logo-mark.png`
  - **متأكد:** كل الصفحات بترجع 200 واللوجو ظاهر في HTML.

- [x] ~~**٢.٥ — تحديث الاسم**~~ ✅ `JoSouk` → `Mawsouq` في `app/layout.tsx` و `(auth)/layout.tsx` و `forgot-password` و `service-center`. وكمان `Mawsouk` (بالـ K) → `Mawsouq` في `messages/{en,ar}/footer.json`.
  - **متأكد:** صفر أثر متبقي لـ `Josouk` أو `logo.svg` في المشروع.

---

## المرحلة ٣ — تنظيف الألوان المكتوبة يدويًا

> ده أكبر جزء في الشغل. **٦٢ ملف** فيهم `#2E4D52` مكتوب يدوي بدل ما يستخدموا التوكن.

- [x] ~~**٣.١ — استبدال التركوازي**~~ ✅ **٦٥٧ تكرار** اتبدلوا. النص → `text-foreground` (ink) · الأزرار والحدود → `primary`.
  - طلع إن اللون كان بيلعب دورين: لون براند **و** لون نص غامق. تحويل كل النصوص لأزرق كان هيكسر قاعدة النسب، فالنصوص راحت لـ ink.

- [x] ~~**٣.٢ — استبدال الأصفر `#FFB814`**~~ ✅ **١٠٦ تكرار** → أزرق.
  - اكتشاف: الأصفر كان مستخدم في خطوط أفقية سميكة مدورة تحت العناوين — ده حرفيًا الـ **sweep** المطلوب في الهوية الجديدة، فالتحويل كان طبيعي.

- [x] ~~**٣.٣ — باقي ألوان الهوية القديمة**~~ ✅
  - `#e6f0f1` (٦٥) → blue tint · `#364153` + `#1d2846` (٨٧) → ink · `#a8babc` + `#c4d3d5` → نص ثانوي/حدود · `#fffaf6` + `#f3eae2` + `#f4f1ea` + `#ece8e2` → surface/muted.
  - **عيلة تانية كاملة اتكشفت متأخر** (بعد ملاحظة إن الهوفر فضل قديم): درجات تركوازية غامقة للهوفر (`#254349` `#1a3035` `#1a2e31` `#101e20` `#0d1f22`) وفاتحة للتينت (`#f4f8f9` `#edf2f2` `#8aabb5` `#88b0be`) وصفرا للهوفر (`#ffc933` `#e6a410`). **٢٩ ملف** إضافي.

- [x] ~~**٣.٤ — مراجعة نسبة الألوان**~~ ✅ اتعملت أثناء التنفيذ، وأدت لـ ٤ قرارات (تحت).

- [x] ~~**٣.٥ — تصحيح الدلالات اللونية**~~ ✅ (مكنش في الخطة الأصلية)
  - بادج **SALE** كان أحمر `#ca0101` → برتقالي `bg-deal`. الأحمر في الباليتة للأخطاء فقط.
  - بادج الخصم `-13%` وسعر التخفيض → `text-deal`.
  - بيل **"New Release"** → أزرق ممتلئ · بيل **"Clearance Sale"** → `bg-deal` (مطابق حرفيًا للـ design system).
  - نجوم التقييم → `--warning` (الأمبر الوحيد في الباليتة) مش الديل أكسنت، عشان متكسرش قاعدة "البرتقالي للخصومات فقط".
  - "مسح الفلاتر" → `text-destructive`.

- [x] ~~**٣.٦ — فحص التباين**~~ ✅ (مكنش في الخطة الأصلية)
  - شريط الإعلانات: نص أسود على أزرق → `text-primary-foreground`.
  - زرار "Join" في الفوتر: نص `#1a1a1a` على أزرق → أبيض. وخانة الإيميل كانت `bg-[#0e1a23]` بحدود بنفس لون الفوتر (مش باينة) → `bg-white/5` + `border-white/15`.
  - عنوان "BEST SELLER": كان أبيض على شريط غامق، والشريط بقى تينت فاتح → النص بقى ink.
  - فحص شامل: صفر حالة نص غامق على خلفية زرقا/برتقالي صلبة.

---

## المرحلة ٤ — الخطوط في الكومبوننتس

- [x] ~~**٤.١ — استبدال `font-satoshi` (١٩ ملف)**~~ ✅ اتبدلوا لـ `font-chillax`.
- [ ] **٤.٢ — استبدال `font-cairo` (٢٨ ملف)** بـ `font-alexandria` — ⏸️ مؤجل بقرار العميل.
- [x] ~~**٤.٣ — استبدال `font-bebas` (٤٥ ملف)**~~ ✅ اتبدلوا لـ `font-chillax`. **إجمالي ٤٧ ملف بقى على `font-chillax`، وصفر متبقي على satoshi أو bebas.**
  - ⚠️ **لسه محتاج مراجعة بصرية** — النقط اللي تحت لسه قايمة:
  - [ ] **مراجعة اللياوت:** Bebas Neue كان خط condensed ضيق، وChillax أعرض بكتير — العناوين في الـ ٤٥ ملف دول هتاخد مساحة أكبر وممكن تكسر السطر. لازم مراجعة بصرية لكل صفحة.
  - [ ] **تحويل النصوص لـ sentence case:** Bebas كان كابيتال بطبيعته فالنصوص مكتوبة على أساس كده. الهوية الجديدة بتفرض sentence case — النص نفسه محتاج يتغير مش الكلاس بس.
- [ ] **٤.٤ — تطبيق سلّم المقاسات** (Hero 48–64 / H1 36 / H2 28 / H3 20 / Body 16 / Label 13) على الكومبوننتس.
- [ ] **٤.٥ — تكبير النص العربي ٥–١٠٪** عن مقابله اللاتيني — يتعمل كقاعدة في `html[lang="ar"]` بدل تعديل كل كومبوننت.

---

## المرحلة ٥ — الكومبوننتس

- [ ] **٥.١ — الأزرار** (Primary أزرق / Secondary أبيض بحد أزرق / Ghost نص أزرق / Destructive أحمر / Disabled رمادي فاتح)
  - hover = تغميق (`#0A2593`)، pressed = `#081F7C`، النيوترال hover → blue tint.
- [ ] **٥.٢ — الـ Badges / Pills**
  - `-30%` و `Sale` → برتقالي (deal accent)
  - `New` → أزرق ممتلئ
  - `Free delivery` / `Warranty` / `Official distributor` / `توصيل مجاني` → blue tint بنص أزرق
- [ ] **٥.٣ — الأسعار** — 18 Semibold · السعر القديم مشطوب رمادي · سعر التخفيض برتقالي · أرقام إنجليزية في اللغتين.
- [ ] **٥.٤ — النجوم والتقييم** + عدد المراجعات بين قوسين.
- [ ] **٥.٥ — أيقونة الكارت مع عدّاد** (badge دائري أزرق).
- [ ] **٥.٦ — كومبوننت الـ Sweep** — خط سفلي سميك مدوّر أزرق. يتعمل ككومبوننت أو يوتيليتي كلاس واحد ويتحط تحت عناوين الأقسام.
- [ ] **٥.١٠ — ووترمارك الفوتر** ([footer.tsx:200](components/layout/footer/footer.tsx#L200)) — النص العملاق أسفل الفوتر. اتقرر يفضل **نص** مش صورة (اللوجو PNG هيبان مهزوز في العرض ده، والتدرج الشفاف `bg-clip-text` مينفعش مع صورة). **قرار معلّق:** يتشال `uppercase` عشان يطابق اللوجو (`mawsouq` حروف صغيرة) وقاعدة الـ sentence case؟ وكمان الـ design system بيقول الـ sweep هو الموتيف الزخرفي **الوحيد** — يعني حرفيًا العنصر ده المفروض يتشال.
- [ ] **٥.٧ — التوستات** (`sonner` مركّب بالفعل في `app/layout.tsx` بـ `richColors`)
  - success/error/warning/info بالألوان الجديدة + أيقونات + دعم RTL.
- [ ] **٥.٨ — الـ Empty states** — أيقونة جوه دايرة blue tint + عنوان + سطر شرح + زرار primary.
- [ ] **٥.٩ — الفورمز** — إنبتس وسيلكتس وسيرش (بزرار أزرق ملتصق) · حالة الخطأ حد أحمر + رسالة تحت · نسخ RTL مظبوطة.

---

## المرحلة ٦ — RTL

> البنية الأساسية موجودة بالفعل: `app/layout.tsx` بيحط `dir` على `<html>` و `<body>` صح.

- [ ] **٦.١ — مراجعة الأيقونات الاتجاهية** — الـ chevrons والأسهم لازم تتقلب في RTL.
- [ ] **٦.٢ — تأكيد إن الأرقام تفضل إنجليزية** في السياق العربي (أسعار، كميات، تواريخ).
- [ ] **٦.٣ — مراجعة الـ padding/margin الاتجاهية** — استخدام `ps-*`/`pe-*` بدل `pl-*`/`pr-*`.
- [ ] **٦.٤ — مراجعة النصوص العربية** — فصحى رسمية، من غير letterspacing أو تمديد.

---

## المرحلة ٧ — الحركة

- [ ] **٧.١ — توحيد كل الـ transitions** على `120–200ms` + `cubic-bezier(.4,0,.2,1)`.
- [ ] **٧.٢ — شيل أي `scale`/bounce من الـ hover states** (الـ design system بيمنعها صراحة).
- [ ] **٧.٣ — راجع `reveal-up` و `hero-ken-burns`** في `globals.css` (بند ١.٧).

---

## المرحلة ٨ — أصول السوشيال ميديا

> ده شغل تصميم مش كود — للفريق التسويقي.

- [ ] **٨.١ — قوالب:** Feed 1080×1350 · Square 1080×1080 · Story 1080×1920.
- [ ] **٨.٢ — هامش أمان 64px** من كل الجهات.
- [ ] **٨.٣ — اللوجو أسفل-يمين** في كل بوست، بمقاس ثابت واحد.
- [ ] **٨.٤ — الستوري:** العلامة أعلى-شمال، والمحتوى بعيد عن أول 250px وآخر 320px.
- [ ] **٨.٥ — عنوان واحد + سطر دعم واحد كحد أقصى + سعر/CTA.**
- [ ] **٨.٦ — العنوان بالعربية الفصحى افتراضيًا**، وسطر إنجليزي صغير اختياري.

---

## المرحلة ٩ — المراجعة النهائية

- [ ] **٩.١** — مراجعة بصرية لكل الصفحات الرئيسية (هوم، ليست المنتجات، صفحة المنتج، الكارت، الشيك أوت، اللوجين، البروفايل) في **العربي والإنجليزي**.
- [ ] **٩.٢** — تأكيد نسبة ٨٠/١٥/٥ في كل صفحة.
- [ ] **٩.٣** — تأكيد إن البرتقالي ظاهر **في سياق العروض والخصومات فقط**.
- [ ] **٩.٤** — فحص تباين الألوان (WCAG AA) — خصوصًا `#5A5F73` على `#F5F6FA`.
- [ ] **٩.٥** — مراجعة على الموبايل.
- [ ] **٩.٦** — تأكيد إن مفيش أي أثر للهوية القديمة: `grep -ri "2e4d52\|ffb814\|satoshi\|cairo\|bebas" --include="*.tsx" --include="*.css" .`

---

## المرحلة ١٠ — البانل (`josouk-panel-v2`) — أولوية أقل

> البانل أداة إدارية داخلية مش واجهة عملاء، فمش لازم يتطبق عليه كل تفاصيل الهوية.

- [ ] **١٠.١** — قرار: البانل يتحوّل للهوية الجديدة ولا يفضل زي ما هو؟
- [ ] **١٠.٢** — لو أيوه: نفس تسلسل المرحلة ١ (توكنات) ثم ٤ (خطوط) ثم ٥ (كومبوننتس).
- [ ] **١٠.٣** — تحديث اللوجو في البانل.

---

## ملاحظات فنية متعلقة

- [ ] **مشكلة hydration mismatch لسه قايمة** — بتظهر في اللوج عند تشغيل الفرونت. مش متعلقة بالبراندينج بس هتلخبط المراجعة البصرية، فيُفضل تتحل الأول.
- [ ] **`.env` و `.env.local` مكررين** بنفس المحتوى — يتشال واحد.
- [ ] **`/en` بيرجع 404** — يتراجع إن كان ده تصميم مقصود ولا مشكلة راوتينج.
